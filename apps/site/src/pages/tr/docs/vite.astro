---
import DocsLayout from '../../../layouts/DocsLayout.astro';
import CodeBlock from '../../../components/ui/CodeBlock.astro';

const host = `import { createNodeHandler } from '@copypatch/node';
import { backend } from './copypatch.js';

const handleCopyPatch = createNodeHandler(backend);
// /__copypatch/api/v2 isteklerini Vite middleware ve SPA fallback öncesinde handleCopyPatch'e verin.`;
---

<DocsLayout title="Vite + Node" description="Vite istemci derlemesini, aynı deployment içindeki Node hostuyla kullanın.">
  <h1>Vite + Node</h1>
  <p class="lead">Vite yalnızca istemciyi derler. Düzenleme için CopyPatch'i uygulamanızı sunan aynı Node sürecine mount edin.</p>
  <CodeBlock code={host} lang="ts" title="server.ts" />
  <p>CopyPatch yolu için Vite development proxy tanımlamayın. Tam statik Vite çıktısı fallback metni gösterebilir, fakat düzenleme çalıştıramaz.</p>
</DocsLayout>
