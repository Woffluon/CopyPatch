---
import DocsLayout from '../../../layouts/DocsLayout.astro';
---

<DocsLayout title="Sorun giderme" description="Same-origin CopyPatch v2 entegrasyonlarındaki yaygın durumlar.">
  <h1>Sorun giderme</h1>
  <table>
    <thead><tr><th>Belirti</th><th>Kontrol</th></tr></thead>
    <tbody>
      <tr><td><code>ORIGIN_REJECTED</code></td><td>Tarayıcı isteğinin uygulamanın tam origininden geldiğini doğrulayın. Ayrı server, proxy ve CORS ayarlarını kaldırın.</td></tr>
      <tr><td><code>CSRF_FAILED</code></td><td>Built-in oturum kullanıyorsanız güncel CSRF tokenı gönderin. Host auth kullanıyorsanız adaptörün mutation doğrulamasını kontrol edin.</td></tr>
      <tr><td><code>REVISION_CONFLICT</code></td><td>Yanıtın <code>latest</code> snapshot'ını yükleyip düzenlemeyi yeniden deneyin.</td></tr>
      <tr><td>Editor görünmüyor</td><td><code>?copypatch=1</code> parametresini, mount edilmiş route'u ve geçerli editor rolünü kontrol edin.</td></tr>
      <tr><td>Sağlık başarısız</td><td>Storage migration ve SQLite/PostgreSQL bağlantısını kontrol edin.</td></tr>
    </tbody>
  </table>
</DocsLayout>
