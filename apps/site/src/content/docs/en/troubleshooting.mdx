---
title: "Troubleshooting & Diagnostics"
description: "Diagnose error codes, middleware ordering issues, database locks, and authentication errors in CopyPatch v3."
group: operations-security
order: 4
---
import CodeBlock from '../../../components/ui/CodeBlock.astro';

export const doctorExample = `# Run runtime diagnostic check
npx copypatch doctor`;

<h1>Troubleshooting & FAQs</h1>
  <p class="lead">
    Diagnose common setup, routing, authentication, and database issues in CopyPatch v3 integrations.
  </p>

  <h2>1. Quick Diagnostic with CLI</h2>
  <p>
    The fastest way to inspect the generated configuration and required CopyPatch environment variables is to run the <code>doctor</code> command:
  </p>
  <CodeBlock code={doctorExample} lang="bash" title="Terminal" />

  <h2>2. Error Codes Reference</h2>
  <table>
    <thead>
      <tr>
        <th>Error Code</th>
        <th>HTTP Status</th>
        <th>Root Cause</th>
        <th>Resolution</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>VALIDATION_ERROR</code></td>
        <td><code>400</code></td>
        <td>A request body, locale, content key, or text value failed validation.</td>
        <td>Correct the request shape and respect the configured text limit.</td>
      </tr>
      <tr>
        <td><code>UNAUTHENTICATED</code></td>
        <td><code>401 / 403</code></td>
        <td>Authentication is missing or the principal lacks the required role.</td>
        <td>Authenticate again and verify the adapter's <code>editor</code> or <code>publisher</code> role mapping.</td>
      </tr>
      <tr>
        <td><code>CSRF_FAILED</code></td>
        <td><code>403</code></td>
        <td>The CSRF token or host mutation verification failed.</td>
        <td>Refresh the session; custom adapters must return <code>true</code> explicitly from <code>verifyMutation</code>.</td>
      </tr>
      <tr>
        <td><code>ORIGIN_REJECTED</code></td>
        <td><code>403</code></td>
        <td>The request <code>Origin</code> does not exactly match its URL origin.</td>
        <td>Use the same-origin API route and review the host's trusted-proxy policy.</td>
      </tr>
      <tr>
        <td><code>RATE_LIMITED</code></td>
        <td><code>429</code></td>
        <td>The unsafe-request limit for the client key was exceeded.</td>
        <td>Wait for the retry window and verify that clients receive distinct trusted addresses.</td>
      </tr>
      <tr>
        <td><code>CLIENT_ADDRESS_UNAVAILABLE</code></td>
        <td><code>503</code></td>
        <td>A Next.js unsafe request has no trusted client address.</td>
        <td>Resolve one from the deployment platform or explicitly opt in to the shared bucket.</td>
      </tr>
      <tr>
        <td><code>REVISION_CONFLICT</code></td>
        <td><code>409</code></td>
        <td>Another editor changed the published or draft revision.</td>
        <td>Reload the editor snapshot and re-apply the change.</td>
      </tr>
      <tr>
        <td><code>UNSUPPORTED_OPERATION</code></td>
        <td><code>405</code></td>
        <td>The endpoint does not support the HTTP method.</td>
        <td>Use the method documented for that endpoint.</td>
      </tr>
      <tr>
        <td><code>INTERNAL_ERROR</code></td>
        <td><code>500</code></td>
        <td>The backend could not complete the request.</td>
        <td>Inspect server logs and storage health without exposing internal details to clients.</td>
      </tr>
      <tr>
        <td><code>NOT_FOUND</code></td>
        <td><code>404</code></td>
        <td>The requested CopyPatch endpoint does not exist.</td>
        <td>Verify the canonical <code>/__copypatch/api/v2</code> mount and route suffix.</td>
      </tr>
    </tbody>
  </table>

  <h2>3. Common Symptoms & Fixes</h2>

  <h3>The Editing Overlay Never Appears</h3>
  <ul>
    <li>Confirm the URL query parameter is exactly <code>?copypatch=1</code>.</li>
    <li>Ensure <code>CopyPatchProvider</code> wraps the active component tree.</li>
    <li>Check the browser Console for JavaScript errors during lazy-loading.</li>
  </ul>

  <h3>Route Returns 404 Not Found</h3>
  <ul>
    <li>Verify that your host router mounts the handler at the exact path: <code>/__copypatch/api/v2</code>.</li>
    <li>In Next.js App Router, ensure the folder name is percent-encoded: <code>app/%5F%5Fcopypatch/api/v2/[...path]/route.ts</code>.</li>
    <li>In Express, ensure <code>app.use('/__copypatch/api/v2', expressMiddleware(backend))</code> is declared <strong>before</strong> <code>express.json()</code> and SPA fallback handlers.</li>
  </ul>

  <h2>4. Frequently Asked Questions (FAQs)</h2>

  <h3>Can I store rich-text or HTML markup in CopyPatch?</h3>
  <p>
    <strong>No.</strong> CopyPatch intentionally adheres to a strict plain-text invariant. Permitting HTML input creates stored XSS risks and allows non-technical editors to break CSS styling and responsive layouts.
  </p>

  <h3>Can I host CopyPatch on a purely static platform (e.g. GitHub Pages)?</h3>
  <p>
    A static export can render default fallback text, but you cannot edit or save copy on static-only hosting. An active Node.js, serverless, or container backend is required to process authenticated mutations and connect to storage.
  </p>
